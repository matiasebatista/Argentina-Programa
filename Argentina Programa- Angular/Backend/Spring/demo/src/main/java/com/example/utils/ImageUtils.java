
//package com.example.utils;

//import java.util.Base64;

//import javax.sql.rowset.serial.SerialBlob;
//import javax.sql.rowset.serial.SerialException;

//import java.sql.SQLException;

//public class ImageUtils {
  //  public static byte[] base64ToBlob(String base64) throws SerialException, SQLException{
    //    String sanitizedBase64 = base64.split(",")[1];
     //   byte[] decodedByte = Base64.getDecoder().decode(sanitizedBase64);
       // return new SerialBlob(decodedByte).getBytes(1, decodedByte.length);
        
   // }
//}

//esto iba a utilizar para enviar las imagenes en la base de datos como Blob